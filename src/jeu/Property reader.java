/*package main.java;

import com.sun.org.apache.xpath.internal.operations.Bool;

import java.io.IOException;
import java.io.InputStream;
import java.util.Properties;
import java.util.ResourceBundle;

import static java.lang.System.getProperty;

public class Propertyreader {


    private Integer essai;
    private Integer combinaison;
    private Boolean developpeur;
    /*
    ** Contructeur java

    public Config(Integer essai, Integer combinaison, Boolean developpeur){
        this.essai = essai;
        this.combinaison = combinaison;
        this.developpeur = developpeur;
    }


    public   readConfig() {


          /*combinaison = new combinaison(getProperty("Combinaison"));
          developpeur = new developpeur(getProperty("Combinaison"));

                       final Properties prop = new Properties();
            InputStream input = null;

            try {

                ResourceBundle config = ResourceBundle.getBundle("config");


                // load a properties file
                prop.load(input);

                // get the property value and print it out
                System.out.println(prop.getProperty("Combinaison"));
                System.out.println(prop.getProperty("Essai"));
                System.out.println(prop.getProperty("Developpeur"));
                essai = getProperty("Essai");
                /*modeDeveloppeur = getProperty("Developpeur");
                nombreEssai = getProperty("Essai");
                /*nombreChiffreCombinaison = getProperty("Combinaison");





            } catch (final IOException ex) {
                ex.printStackTrace();
            } finally {
                if (input != null) {
                    try {
                        input.close();
                    } catch (final IOException e) {
                        e.printStackTrace();
                    }
                }
            }

    }
    public config(int essai, int combinaison, boolean developpeur) {
        this.essai = Integer.decode(essai);
        this.combinaison = Integer.decode(nombreChiffreCombinaison);
        this.developpeur = Boolean.parseBoolean(modeDeveloppeur);
    }

    public Integer getEssai() {
        return essai;
    }

    public boolean isDeveloppeur() {
        return developpeur;
    }

    public int getCombinaison() {
        return combinaison;
    }
}*/